#include <bits/stdc++.h>
#include <iostream>
using namespace std;
int a,b;

void solve() {
    cin >> a >> b;
    cout << a+b << endl;
}

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);
    int t; cin >> t;
    while(t--) {
        solve();
    }

    return 0;
}