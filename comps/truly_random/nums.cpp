#include <bits/stdc++.h>
#define int long long

using namespace std;
int a,b;

signed main() {
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);
    cin >> a >> b;
    for (int a; a <= b; a++) {
        cout << a << " ";
    }
    return 0;
}
//-std=c++17
//-std=c++11