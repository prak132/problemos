#include <bits/stdc++.h>
using namespace std;
typedef long long ll;

void solve() {
    ll N, M, K; cin >> N >> M >> K;
    cout << "NO" << endl;
}

int main() {
    ios_base::sync_with_stdio(false);cin.tie(nullptr);
    int t=1; 
    // cin >> t;
    while (t--) {solve();}
    return 0;
}